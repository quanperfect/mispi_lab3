<?xml version="1.0"?>
<project name="lab3" default="compile">


    <property file="build.properties"/>

<!--    <property name="git.macro" location="dop_task/build.xml"/>-->
<!--    <import file="${git.macro}"/>-->

    <path id="classpath">
        <fileset dir="${lib.dir}" includes="*.jar"/>
    </path>

    <target name="compile">
        <mkdir dir="${classes.dir}"/>
        <javac srcdir="${src.dir}" nowarn="on" destdir="${classes.dir}" classpathref="classpath"
               includeantruntime="false"/>
    </target>

    <target name="build" depends="compile">
        <mkdir dir="${jar.dir}"/>
        <jar destfile="${jar.dir}/${ant.project.name}.jar" basedir="${classes.dir}" excludes="**/*Test.class">
            <manifest>
                <attribute name="Main-Class" value="${main.class}"/>
            </manifest>
        </jar>
    </target>

    <target name="clean">
        <delete dir="${build.dir}"/>
    </target>

    <target name="test" depends="build">
        <junit printsummary="yes">
            <formatter type="brief" usefile="false"/>

            <classpath location="${lib.dir}/junit-3.8.2.jar"/>
            <classpath>
                <pathelement location="${classes.dir}"/>
            </classpath>
            <test name="com.example.Web3.utils.HitCheckerTest"/>
            <test name="com.example.Web3.utils.ValidatorTest"/>
        </junit>
    </target>

    <target name="xml">
        <xmlvalidate failonerror="no" lenient="yes" warn="yes"
                     classname="org.apache.xerces.parsers.SAXParser"
            classpath="${lib.dir}/xercesImpl-2.4.0.jar">
            <fileset dir="src" includes="**/*.xml"/>
        </xmlvalidate>
    </target>

    <target

</project>